#include "main.h"

/**
 * _isdigit - entry point
 * check for digit 0 through 9
 * @c: variable
 * Return: 1 or 0
 */

int _isdigit(int c)
{
	if _isdigit(c)
	{
		_putchar("%c: %d\n", c, _isdigit(c));
		return (1);
	}
	else
	{
		_putchar("%c: %d\n", c, _isdigit(c));
		return (0);
	}
}
